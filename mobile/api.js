// Update this URL if you're running the backend on a different machine or
// testing on a physical device. For emulator:
// - Android emulator (default AVD): use 10.0.2.2 instead of localhost
// - iOS simulator: localhost normally works
// Or set to your dev machine IP: "http://192.168.x.y:4000"
const API_BASE = 'http://localhost:4000/api';

export default API_BASE;
